# Connect to Supabase via connection pooling
DATABASE_URL=""

# Direct connection to the database. Used for migrations
DIRECT_URL=""

PORT = 3333

# CONFIGURAÇÃO DO TOKEN JWT
JWT_SECRET = secret
JWT_TOKEN_AUDIENCE = http://localhost:3333
JWT_TOKEN_ISSUER = http://localhost:3333
JWT_TTL = "5h"

ENCRYPTION_KEY=""